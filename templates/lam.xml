<?xml version="1.0"?>
<Container version="2">
  <Name>LAM</Name>
  <Repository>ldapaccountmanager/lam</Repository>
  <Registry>https://hub.docker.com/r/ldapaccountmanager/lam/</Registry>
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <Support>https://hub.docker.com/r/ldapaccountmanager/lam/</Support>
  <Project>https://hub.docker.com/r/ldapaccountmanager/lam</Project>
  <Overview>LDAP Account Manager (LAM) is a webfrontend for managing entries (e.g. users, groups, DHCP settings) stored in an LDAP directory. LAM was designed to make LDAP management as easy as possible for the user. It abstracts from the technical details of LDAP and allows persons without technical background to manage LDAP entries. If needed, power users may still directly edit LDAP entries via the integrated LDAP browser.</Overview>
  <Category>Tools:</Category>
  <Icon>https://raw.githubusercontent.com/C4ArtZ/Unraid-Templates/lam/templates/icons/lam.png</Icon>
  <Description>LDAP Account Manager (LAM) is a webfrontend for managing entries (e.g. users, groups, DHCP settings) stored in an LDAP directory. LAM was designed to make LDAP management as easy as possible for the user. It abstracts from the technical details of LDAP and allows persons without technical background to manage LDAP entries. If needed, power users may still directly edit LDAP entries via the integrated LDAP browser.</Description>
  <Config Name="Config path" Target="/var/lib/ldap-account-manager/config" Default="/mnt/user/appdata/lam" Mode="rw" Description="Container Path: /var/lib/ldap-account-manager/config" Type="Path" Display="always" Required="true" Mask="false"/>
  <Config Name="Port" Target="80" Default="8080" Mode="tcp" Description="Container Port: 80" Type="Port" Display="always" Required="true" Mask="false"/>
  <Config Name="Skip preconfiguration" Target="LAM_SKIP_PRECONFIGURE" Default="true" Mode="" Description="Set to 'false' if you want lam to create a config file.&#13;&#10;Default is 'true' because you are expected to supply one above." Type="Variable" Display="always" Required="false" Mask="false"/>
  <Config Name="Virtual host" Target="VIRTUAL_HOST" Default="" Mode="" Description="Add your FQDN here if you want to use the nginx proxy." Type="Variable" Display="always" Required="false" Mask="false"/>
  <Config Name="Virtual port" Target="VIRTUAL_PORT" Default="" Mode="" Description="Specify if you are using the nginx proxy." Type="Variable" Display="always" Required="false" Mask="false"/>
</Container>